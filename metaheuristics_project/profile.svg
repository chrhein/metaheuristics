<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="214" onload="init(evt)" viewBox="0 0 1200 214" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = true;
var searchcolor = 'rgb(230,0,230)';
var fluiddrawing = true;
var truncate_text_right = false;]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
            var params = get_params()
            params.x = el.attributes._orig_x.value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = format_percent((parseFloat(e.attributes.x.value) - x) * ratio);
            if (e.tagName == "text") {
                e.attributes.x.value = format_percent(parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value) + (100 * 3 / frames.attributes.width.value));
            }
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = format_percent(parseFloat(e.attributes.width.value) * ratio);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    var ratio = 100 / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.001;
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;    // JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="214" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">py-spy</text><text id="details" x="10" y="197.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="197.00"> </text><svg id="frames" x="10" width="1180"><g><title>operator (alns.py:176) (633 samples, 3.49%)</title><rect x="0.1544%" y="116" width="3.4915%" height="15" fill="rgb(227,0,7)"/><text x="0.4044%" y="126.50">ope..</text></g><g><title>check_solution (feasibility_check.py:58) (541 samples, 2.98%)</title><rect x="6.6244%" y="148" width="2.9840%" height="15" fill="rgb(217,0,24)"/><text x="6.8744%" y="158.50">che..</text></g><g><title>update_weights (alns.py:155) (1,079 samples, 5.95%)</title><rect x="3.6680%" y="132" width="5.9515%" height="15" fill="rgb(221,193,54)"/><text x="3.9180%" y="142.50">update_w..</text></g><g><title>update_weights (alns.py:156) (849 samples, 4.68%)</title><rect x="9.6194%" y="132" width="4.6828%" height="15" fill="rgb(248,212,6)"/><text x="9.8694%" y="142.50">updat..</text></g><g><title>f (cost_calculation.py:61) (848 samples, 4.68%)</title><rect x="9.6249%" y="148" width="4.6773%" height="15" fill="rgb(208,68,35)"/><text x="9.8749%" y="158.50">f (co..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (183 samples, 1.01%)</title><rect x="14.5339%" y="164" width="1.0094%" height="15" fill="rgb(232,128,0)"/><text x="14.7839%" y="174.50"></text></g><g><title>operator (alns.py:177) (2,829 samples, 15.60%)</title><rect x="3.6459%" y="116" width="15.6040%" height="15" fill="rgb(207,160,47)"/><text x="3.8959%" y="126.50">operator (alns.py:177)</text></g><g><title>update_weights (alns.py:163) (888 samples, 4.90%)</title><rect x="14.3519%" y="132" width="4.8980%" height="15" fill="rgb(228,23,34)"/><text x="14.6019%" y="142.50">update..</text></g><g><title>f (cost_calculation.py:61) (880 samples, 4.85%)</title><rect x="14.3960%" y="148" width="4.8538%" height="15" fill="rgb(218,30,26)"/><text x="14.6460%" y="158.50">f (cos..</text></g><g><title>adaptive_large_neighborhood_search (alns.py:122) (3,465 samples, 19.11%)</title><rect x="0.1489%" y="100" width="19.1120%" height="15" fill="rgb(220,122,19)"/><text x="0.3989%" y="110.50">adaptive_large_neighborhood_se..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (361 samples, 1.99%)</title><rect x="19.5036%" y="132" width="1.9912%" height="15" fill="rgb(250,228,42)"/><text x="19.7536%" y="142.50">t..</text></g><g><title>total_cost_calc (cost_calculation.py:41) (278 samples, 1.53%)</title><rect x="25.1351%" y="132" width="1.5334%" height="15" fill="rgb(240,193,28)"/><text x="25.3851%" y="142.50"></text></g><g><title>adaptive_large_neighborhood_search (alns.py:125) (1,632 samples, 9.00%)</title><rect x="19.2664%" y="100" width="9.0017%" height="15" fill="rgb(216,20,37)"/><text x="19.5164%" y="110.50">adaptive_larg..</text></g><g><title>f (cost_calculation.py:61) (1,631 samples, 9.00%)</title><rect x="19.2719%" y="116" width="8.9961%" height="15" fill="rgb(206,188,39)"/><text x="19.5219%" y="126.50">f (cost_calcu..</text></g><g><title>check_solution (feasibility_check.py:20) (342 samples, 1.89%)</title><rect x="28.6211%" y="116" width="1.8864%" height="15" fill="rgb(217,207,13)"/><text x="28.8711%" y="126.50">c..</text></g><g><title>__init__ (collections/__init__.py:568) (193 samples, 1.06%)</title><rect x="29.4429%" y="132" width="1.0645%" height="15" fill="rgb(231,73,38)"/><text x="29.6929%" y="142.50"></text></g><g><title>time_calc (time_calculation.py:15) (184 samples, 1.01%)</title><rect x="34.1258%" y="132" width="1.0149%" height="15" fill="rgb(225,20,46)"/><text x="34.3758%" y="142.50"></text></g><g><title>check_solution (feasibility_check.py:58) (934 samples, 5.15%)</title><rect x="33.8500%" y="116" width="5.1517%" height="15" fill="rgb(210,31,41)"/><text x="34.1000%" y="126.50">check_..</text></g><g><title>adaptive_large_neighborhood_search (alns.py:129) (1,941 samples, 10.71%)</title><rect x="28.3232%" y="100" width="10.7060%" height="15" fill="rgb(221,200,47)"/><text x="28.5732%" y="110.50">adaptive_large_n..</text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:20) (480 samples, 2.65%)</title><rect x="39.1285%" y="116" width="2.6475%" height="15" fill="rgb(226,26,5)"/><text x="39.3785%" y="126.50">ps..</text></g><g><title>f (cost_calculation.py:61) (479 samples, 2.64%)</title><rect x="39.1340%" y="132" width="2.6420%" height="15" fill="rgb(249,33,26)"/><text x="39.3840%" y="142.50">f ..</text></g><g><title>fast_reinsert (one_reinsert.py:10) (297 samples, 1.64%)</title><rect x="42.0408%" y="132" width="1.6382%" height="15" fill="rgb(235,183,28)"/><text x="42.2908%" y="142.50"></text></g><g><title>&lt;listcomp&gt; (one_reinsert.py:10) (273 samples, 1.51%)</title><rect x="42.1732%" y="148" width="1.5058%" height="15" fill="rgb(221,5,38)"/><text x="42.4232%" y="158.50"></text></g><g><title>check_solution (feasibility_check.py:20) (746 samples, 4.11%)</title><rect x="45.5709%" y="148" width="4.1147%" height="15" fill="rgb(247,18,42)"/><text x="45.8209%" y="158.50">chec..</text></g><g><title>__init__ (collections/__init__.py:568) (435 samples, 2.40%)</title><rect x="47.2863%" y="164" width="2.3993%" height="15" fill="rgb(241,131,45)"/><text x="47.5363%" y="174.50">__..</text></g><g><title>check_solution (feasibility_check.py:40) (393 samples, 2.17%)</title><rect x="51.9140%" y="148" width="2.1677%" height="15" fill="rgb(249,31,29)"/><text x="52.1640%" y="158.50">c..</text></g><g><title>check_solution (feasibility_check.py:58) (481 samples, 2.65%)</title><rect x="56.5913%" y="148" width="2.6531%" height="15" fill="rgb(225,111,53)"/><text x="56.8413%" y="158.50">ch..</text></g><g><title>fast_reinsert (one_reinsert.py:13) (2,624 samples, 14.47%)</title><rect x="44.8042%" y="132" width="14.4732%" height="15" fill="rgb(238,160,17)"/><text x="45.0542%" y="142.50">fast_reinsert (one_rei..</text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:22) (3,349 samples, 18.47%)</title><rect x="41.7981%" y="116" width="18.4721%" height="15" fill="rgb(214,148,48)"/><text x="42.0481%" y="126.50">pseudo_random_one_reinsert (o..</text></g><g><title>calls_to_nodes (route_handler.py:10) (208 samples, 1.15%)</title><rect x="62.2835%" y="164" width="1.1473%" height="15" fill="rgb(232,36,49)"/><text x="62.5335%" y="174.50"></text></g><g><title>calls_to_nodes (route_handler.py:8) (347 samples, 1.91%)</title><rect x="66.1776%" y="164" width="1.9140%" height="15" fill="rgb(209,103,24)"/><text x="66.4276%" y="174.50">c..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (1,536 samples, 8.47%)</title><rect x="61.4837%" y="148" width="8.4721%" height="15" fill="rgb(229,88,8)"/><text x="61.7337%" y="158.50">total_cost_c..</text></g><g><title>calls_to_nodes (route_handler.py:9) (338 samples, 1.86%)</title><rect x="68.0916%" y="164" width="1.8643%" height="15" fill="rgb(213,181,19)"/><text x="68.3416%" y="174.50">c..</text></g><g><title>total_cost_calc (cost_calculation.py:22) (295 samples, 1.63%)</title><rect x="70.6178%" y="148" width="1.6271%" height="15" fill="rgb(254,191,54)"/><text x="70.8678%" y="158.50"></text></g><g><title>total_cost_calc (cost_calculation.py:29) (767 samples, 4.23%)</title><rect x="74.2802%" y="148" width="4.2306%" height="15" fill="rgb(241,83,37)"/><text x="74.5302%" y="158.50">total..</text></g><g><title>total_cost_calc (cost_calculation.py:35) (385 samples, 2.12%)</title><rect x="81.3017%" y="148" width="2.1236%" height="15" fill="rgb(233,36,39)"/><text x="81.5517%" y="158.50">t..</text></g><g><title>total_cost_calc (cost_calculation.py:38) (310 samples, 1.71%)</title><rect x="84.3298%" y="148" width="1.7099%" height="15" fill="rgb(226,3,54)"/><text x="84.5798%" y="158.50"></text></g><g><title>total_cost_calc (cost_calculation.py:41) (1,189 samples, 6.56%)</title><rect x="86.7347%" y="148" width="6.5582%" height="15" fill="rgb(245,192,40)"/><text x="86.9847%" y="158.50">total_cos..</text></g><g><title>get_routes_as_list (route_handler.py:103) (385 samples, 2.12%)</title><rect x="96.0011%" y="164" width="2.1236%" height="15" fill="rgb(238,167,29)"/><text x="96.2511%" y="174.50">g..</text></g><g><title>f (cost_calculation.py:61) (7,117 samples, 39.26%)</title><rect x="60.3585%" y="132" width="39.2554%" height="15" fill="rgb(232,182,51)"/><text x="60.6085%" y="142.50">f (cost_calculation.py:61)</text></g><g><title>total_cost_calc (cost_calculation.py:9) (712 samples, 3.93%)</title><rect x="95.6867%" y="148" width="3.9272%" height="15" fill="rgb(231,60,39)"/><text x="95.9367%" y="158.50">tota..</text></g><g><title>get_routes_as_list (route_handler.py:99) (186 samples, 1.03%)</title><rect x="98.5880%" y="164" width="1.0259%" height="15" fill="rgb(208,69,12)"/><text x="98.8380%" y="174.50"></text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:23) (7,134 samples, 39.35%)</title><rect x="60.2703%" y="116" width="39.3491%" height="15" fill="rgb(235,93,37)"/><text x="60.5203%" y="126.50">pseudo_random_one_reinsert (one_reinsert.py:23)</text></g><g><title>adaptive_large_neighborhood_search (alns.py:78) (10,972 samples, 60.52%)</title><rect x="39.1175%" y="100" width="60.5185%" height="15" fill="rgb(213,116,39)"/><text x="39.3675%" y="110.50">adaptive_large_neighborhood_search (alns.py:78)</text></g><g><title>&lt;module&gt; (main.py:27) (18,107 samples, 99.87%)</title><rect x="0.0717%" y="52" width="99.8731%" height="15" fill="rgb(222,207,29)"/><text x="0.3217%" y="62.50">&lt;module&gt; (main.py:27)</text></g><g><title>main (main.py:23) (18,107 samples, 99.87%)</title><rect x="0.0717%" y="68" width="99.8731%" height="15" fill="rgb(206,96,30)"/><text x="0.3217%" y="78.50">main (main.py:23)</text></g><g><title>alns_init (alns_init.py:28) (18,107 samples, 99.87%)</title><rect x="0.0717%" y="84" width="99.8731%" height="15" fill="rgb(218,138,4)"/><text x="0.3217%" y="94.50">alns_init (alns_init.py:28)</text></g><g><title>all (18,130 samples, 100%)</title><rect x="0.0000%" y="36" width="100.0000%" height="15" fill="rgb(250,191,14)"/><text x="0.2500%" y="46.50"></text></g></svg></svg>