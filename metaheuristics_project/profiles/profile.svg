<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="214" onload="init(evt)" viewBox="0 0 1200 214" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = true;
var searchcolor = 'rgb(230,0,230)';
var fluiddrawing = true;
var truncate_text_right = false;]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
            var params = get_params()
            params.x = el.attributes._orig_x.value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = format_percent((parseFloat(e.attributes.x.value) - x) * ratio);
            if (e.tagName == "text") {
                e.attributes.x.value = format_percent(parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value) + (100 * 3 / frames.attributes.width.value));
            }
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = format_percent(parseFloat(e.attributes.width.value) * ratio);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    var ratio = 100 / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.001;
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;    // JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="214" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">py-spy</text><text id="details" x="10" y="197.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="197.00"> </text><svg id="frames" x="10" width="1180"><g><title>operator (alns.py:159) (681 samples, 3.03%)</title><rect x="0.0490%" y="116" width="3.0311%" height="15" fill="rgb(227,0,7)"/><text x="0.2990%" y="126.50">ope..</text></g><g><title>check_solution (feasibility_check.py:58) (632 samples, 2.81%)</title><rect x="5.8308%" y="148" width="2.8130%" height="15" fill="rgb(217,0,24)"/><text x="6.0808%" y="158.50">ch..</text></g><g><title>update_weights (alns.py:138) (1,253 samples, 5.58%)</title><rect x="3.0934%" y="132" width="5.5771%" height="15" fill="rgb(221,193,54)"/><text x="3.3434%" y="142.50">update_..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (253 samples, 1.13%)</title><rect x="8.8263%" y="164" width="1.1261%" height="15" fill="rgb(248,212,6)"/><text x="9.0763%" y="174.50"></text></g><g><title>update_weights (alns.py:139) (1,124 samples, 5.00%)</title><rect x="8.6705%" y="132" width="5.0029%" height="15" fill="rgb(208,68,35)"/><text x="8.9205%" y="142.50">update..</text></g><g><title>f (cost_calculation.py:61) (1,120 samples, 4.99%)</title><rect x="8.6883%" y="148" width="4.9851%" height="15" fill="rgb(232,128,0)"/><text x="8.9383%" y="158.50">f (cos..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (233 samples, 1.04%)</title><rect x="13.8470%" y="164" width="1.0371%" height="15" fill="rgb(207,160,47)"/><text x="14.0970%" y="174.50"></text></g><g><title>f (cost_calculation.py:61) (1,034 samples, 4.60%)</title><rect x="13.7268%" y="148" width="4.6023%" height="15" fill="rgb(228,23,34)"/><text x="13.9768%" y="158.50">f (co..</text></g><g><title>adaptive_large_neighborhood_search (alns.py:105) (4,112 samples, 18.30%)</title><rect x="0.0312%" y="100" width="18.3024%" height="15" fill="rgb(218,30,26)"/><text x="0.2812%" y="110.50">adaptive_large_neighborhood_s..</text></g><g><title>operator (alns.py:160) (3,427 samples, 15.25%)</title><rect x="3.0801%" y="116" width="15.2535%" height="15" fill="rgb(220,122,19)"/><text x="3.3301%" y="126.50">operator (alns.py:160)</text></g><g><title>update_weights (alns.py:146) (1,036 samples, 4.61%)</title><rect x="13.7223%" y="132" width="4.6112%" height="15" fill="rgb(250,228,42)"/><text x="13.9723%" y="142.50">updat..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (387 samples, 1.72%)</title><rect x="18.5650%" y="132" width="1.7225%" height="15" fill="rgb(240,193,28)"/><text x="18.8150%" y="142.50"></text></g><g><title>total_cost_calc (cost_calculation.py:41) (270 samples, 1.20%)</title><rect x="23.8127%" y="132" width="1.2018%" height="15" fill="rgb(216,20,37)"/><text x="24.0627%" y="142.50"></text></g><g><title>adaptive_large_neighborhood_search (alns.py:108) (1,809 samples, 8.05%)</title><rect x="18.3380%" y="100" width="8.0518%" height="15" fill="rgb(206,188,39)"/><text x="18.5880%" y="110.50">adaptive_la..</text></g><g><title>f (cost_calculation.py:61) (1,809 samples, 8.05%)</title><rect x="18.3380%" y="116" width="8.0518%" height="15" fill="rgb(217,207,13)"/><text x="18.5880%" y="126.50">f (cost_cal..</text></g><g><title>check_solution (feasibility_check.py:20) (366 samples, 1.63%)</title><rect x="26.7236%" y="116" width="1.6291%" height="15" fill="rgb(231,73,38)"/><text x="26.9736%" y="126.50"></text></g><g><title>check_solution (feasibility_check.py:58) (1,188 samples, 5.29%)</title><rect x="31.4283%" y="116" width="5.2878%" height="15" fill="rgb(225,20,46)"/><text x="31.6783%" y="126.50">check_..</text></g><g><title>adaptive_large_neighborhood_search (alns.py:112) (2,316 samples, 10.31%)</title><rect x="26.4432%" y="100" width="10.3085%" height="15" fill="rgb(210,31,41)"/><text x="26.6932%" y="110.50">adaptive_large_..</text></g><g><title>f (cost_calculation.py:61) (598 samples, 2.66%)</title><rect x="36.8452%" y="132" width="2.6617%" height="15" fill="rgb(221,200,47)"/><text x="37.0952%" y="142.50">f ..</text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:20) (600 samples, 2.67%)</title><rect x="36.8407%" y="116" width="2.6706%" height="15" fill="rgb(226,26,5)"/><text x="37.0907%" y="126.50">ps..</text></g><g><title>fast_reinsert (one_reinsert.py:10) (372 samples, 1.66%)</title><rect x="39.7294%" y="132" width="1.6558%" height="15" fill="rgb(249,33,26)"/><text x="39.9794%" y="142.50"></text></g><g><title>&lt;listcomp&gt; (one_reinsert.py:10) (341 samples, 1.52%)</title><rect x="39.8674%" y="148" width="1.5178%" height="15" fill="rgb(235,183,28)"/><text x="40.1174%" y="158.50"></text></g><g><title>check_solution (feasibility_check.py:20) (871 samples, 3.88%)</title><rect x="43.2768%" y="148" width="3.8768%" height="15" fill="rgb(221,5,38)"/><text x="43.5268%" y="158.50">chec..</text></g><g><title>__init__ (collections/__init__.py:568) (504 samples, 2.24%)</title><rect x="44.9103%" y="164" width="2.2433%" height="15" fill="rgb(247,18,42)"/><text x="45.1603%" y="174.50">_..</text></g><g><title>check_solution (feasibility_check.py:40) (551 samples, 2.45%)</title><rect x="49.2456%" y="148" width="2.4525%" height="15" fill="rgb(241,131,45)"/><text x="49.4956%" y="158.50">ch..</text></g><g><title>check_solution (feasibility_check.py:58) (478 samples, 2.13%)</title><rect x="54.2039%" y="148" width="2.1276%" height="15" fill="rgb(249,31,29)"/><text x="54.4539%" y="158.50">c..</text></g><g><title>fast_reinsert (one_reinsert.py:13) (3,089 samples, 13.75%)</title><rect x="42.6047%" y="132" width="13.7491%" height="15" fill="rgb(225,111,53)"/><text x="42.8547%" y="142.50">fast_reinsert (one_re..</text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:22) (3,970 samples, 17.67%)</title><rect x="39.5335%" y="116" width="17.6704%" height="15" fill="rgb(238,160,17)"/><text x="39.7835%" y="126.50">pseudo_random_one_reinsert ..</text></g><g><title>calls_to_nodes (route_handler.py:10) (336 samples, 1.50%)</title><rect x="59.2246%" y="164" width="1.4955%" height="15" fill="rgb(214,148,48)"/><text x="59.4746%" y="174.50"></text></g><g><title>calls_to_nodes (route_handler.py:12) (225 samples, 1.00%)</title><rect x="61.4323%" y="164" width="1.0015%" height="15" fill="rgb(232,36,49)"/><text x="61.6823%" y="174.50"></text></g><g><title>calls_to_nodes (route_handler.py:8) (451 samples, 2.01%)</title><rect x="63.6979%" y="164" width="2.0074%" height="15" fill="rgb(209,103,24)"/><text x="63.9479%" y="174.50">c..</text></g><g><title>total_cost_calc (cost_calculation.py:19) (2,021 samples, 9.00%)</title><rect x="58.4902%" y="148" width="8.9954%" height="15" fill="rgb(229,88,8)"/><text x="58.7402%" y="158.50">total_cost_ca..</text></g><g><title>calls_to_nodes (route_handler.py:9) (400 samples, 1.78%)</title><rect x="65.7053%" y="164" width="1.7804%" height="15" fill="rgb(213,181,19)"/><text x="65.9553%" y="174.50">c..</text></g><g><title>total_cost_calc (cost_calculation.py:22) (330 samples, 1.47%)</title><rect x="68.0999%" y="148" width="1.4688%" height="15" fill="rgb(254,191,54)"/><text x="68.3499%" y="158.50"></text></g><g><title>total_cost_calc (cost_calculation.py:29) (1,043 samples, 4.64%)</title><rect x="72.1592%" y="148" width="4.6424%" height="15" fill="rgb(241,83,37)"/><text x="72.4092%" y="158.50">total..</text></g><g><title>total_cost_calc (cost_calculation.py:33) (240 samples, 1.07%)</title><rect x="78.1991%" y="148" width="1.0682%" height="15" fill="rgb(233,36,39)"/><text x="78.4491%" y="158.50"></text></g><g><title>total_cost_calc (cost_calculation.py:35) (566 samples, 2.52%)</title><rect x="80.0107%" y="148" width="2.5193%" height="15" fill="rgb(226,3,54)"/><text x="80.2607%" y="158.50">to..</text></g><g><title>total_cost_calc (cost_calculation.py:38) (439 samples, 1.95%)</title><rect x="83.5270%" y="148" width="1.9540%" height="15" fill="rgb(245,192,40)"/><text x="83.7770%" y="158.50">t..</text></g><g><title>total_cost_calc (cost_calculation.py:41) (1,442 samples, 6.42%)</title><rect x="86.3845%" y="148" width="6.4183%" height="15" fill="rgb(238,167,29)"/><text x="86.6345%" y="158.50">total_co..</text></g><g><title>get_routes_as_list (route_handler.py:103) (550 samples, 2.45%)</title><rect x="95.5001%" y="164" width="2.4480%" height="15" fill="rgb(232,182,51)"/><text x="95.7501%" y="174.50">ge..</text></g><g><title>f (cost_calculation.py:61) (9,505 samples, 42.31%)</title><rect x="57.3285%" y="132" width="42.3065%" height="15" fill="rgb(231,60,39)"/><text x="57.5785%" y="142.50">f (cost_calculation.py:61)</text></g><g><title>total_cost_calc (cost_calculation.py:9) (994 samples, 4.42%)</title><rect x="95.2108%" y="148" width="4.4243%" height="15" fill="rgb(208,69,12)"/><text x="95.4608%" y="158.50">total..</text></g><g><title>get_routes_as_list (route_handler.py:99) (247 samples, 1.10%)</title><rect x="98.5356%" y="164" width="1.0994%" height="15" fill="rgb(235,93,37)"/><text x="98.7856%" y="174.50"></text></g><g><title>pseudo_random_one_reinsert (one_reinsert.py:23) (9,535 samples, 42.44%)</title><rect x="57.2039%" y="116" width="42.4400%" height="15" fill="rgb(213,116,39)"/><text x="57.4539%" y="126.50">pseudo_random_one_reinsert (one_reinsert.py:23)</text></g><g><title>adaptive_large_neighborhood_search (alns.py:59) (14,116 samples, 62.83%)</title><rect x="36.8318%" y="100" width="62.8299%" height="15" fill="rgb(222,207,29)"/><text x="37.0818%" y="110.50">adaptive_large_neighborhood_search (alns.py:59)</text></g><g><title>&lt;module&gt; (main.py:27) (22,459 samples, 99.96%)</title><rect x="0.0178%" y="52" width="99.9644%" height="15" fill="rgb(206,96,30)"/><text x="0.2678%" y="62.50">&lt;module&gt; (main.py:27)</text></g><g><title>main (main.py:23) (22,459 samples, 99.96%)</title><rect x="0.0178%" y="68" width="99.9644%" height="15" fill="rgb(218,138,4)"/><text x="0.2678%" y="78.50">main (main.py:23)</text></g><g><title>alns_init (alns_init.py:28) (22,459 samples, 99.96%)</title><rect x="0.0178%" y="84" width="99.9644%" height="15" fill="rgb(250,191,14)"/><text x="0.2678%" y="94.50">alns_init (alns_init.py:28)</text></g><g><title>all (22,467 samples, 100%)</title><rect x="0.0000%" y="36" width="100.0000%" height="15" fill="rgb(239,60,40)"/><text x="0.2500%" y="46.50"></text></g></svg></svg>